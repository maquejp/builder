# ───────────────────────────────
# 📦 Registry & Security
# ───────────────────────────────

# Always install from the public npm registry
registry=https://registry.npmjs.org/

# Disable audit and fund messages for cleaner output
audit=false
fund=false

# ───────────────────────────────
# 🔧 Dependency Management
# ───────────────────────────────

# Exclude optional dependencies to prevent warnings and reduce installation size
optional=false

# Install exactly the versions specified (no ^ or ~ prefixes for reproducible builds)
save-exact=true

# ───────────────────────────────
# 📄 Lockfile Configuration
# ───────────────────────────────

# Always generate and respect the package-lock.json file
package-lock=true

# Use the modern lockfile format (v3 works best with npm 7+)
lockfile-version=3

# Allow lockfile updates during development (set to true for production)
frozen-lockfile=false

# ───────────────────────────────
# ⚡ Performance & Caching
# ───────────────────────────────

# Prefer local cache to speed up installations
prefer-offline=true

# Reduce network requests by preferring cached packages
prefer-online=false
